<h2>1. What is the difference between var, let, and const?</h2>
<p>Var is function scoped and let,const are block scoped. Var is old way and let,const is the updated and new way. In let,const const can not be changed later it is something make the variable's value constant behaviour.</p>
<h2>2. What is the difference between map(), forEach(), and filter()?</h2>
<p>All of the methods loops though an array but in return the give different type of value. forEach() and filter() does not return any specific value they retun somthing we want to and filter just filters the arry as out given instruction on the other hand map() loops through the array and return a new array.</p>
<h2>3. What are arrow functions in ES6?</h2>
<p>Arrow function is the shorter and updated way to declare or write a function.</p>
<h2>4. How does destructuring assignment work in ES6?</h2>
<p>Destructuring lets us pull values out of arrays or objects and assign them to variables in a cleaner way, instead of accessing each one manually.</p>
<h2>5. Explain template literals in ES6. How are they different from string concatenation?</h2>
<p>It is the easier and updated way to declare or print the output and this makes the concatenation easy then previous way. It uses the backticks to declare all variable together.</p>
